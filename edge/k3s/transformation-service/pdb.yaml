---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: transformation-service-pdb
  namespace: iot-edge
  labels:
    app: transformation-service
spec:
  minAvailable: 1  # At least 1 pod must be available during updates/evictions
  selector:
    matchLabels:
      app: transformation-service
