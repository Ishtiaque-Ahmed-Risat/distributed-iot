---
apiVersion: v1
kind: Service
metadata:
  name: device-registry
  namespace: iot-edge
  labels:
    app: device-registry
spec:
  type: NodePort
  selector:
    app: device-registry
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 31080
    protocol: TCP
